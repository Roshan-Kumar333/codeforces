#include<bits/stdc++.h>
using namespace std;
#define ll long long
void solve(){
    ll n;
    cin >> n;
    vector<vector<ll>> a(n, vector<ll>(3));
    for (ll i = 0; i < n;i++){
        cin >> a[i][0] >> a[i][1] >> a[i][2];
    }
    ll cost_maxi = INT_MAX, cost_mini = INT_MAX, maxi = INT_MIN, mini = INT_MAX;
    bool isSame = false;
    for (ll i = 0; i < n;i++){
        
    }
    return;
}
int main(){
    ll t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}